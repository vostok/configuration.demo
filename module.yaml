full-build *default:
  build:
    target: Vostok.Configuration.Demo.sln
    configuration: Release
    tool:
      name: dotnet

  deps:
    - vostok.devtools
    - vostok.configuration
    - vostok.configuration.sources
    - vostok.configuration.sources.json
